<% provide(:title, "New post") %>
<% provide(:intro, "新しい投稿を作成できます") %>

<%= form_for(@post) do |f| %>
  <%= f.label :content %>
  <%= f.text_area :content, class: "form-control" %>
  <%= f.submit "Create Post", class: "btn btn-primary mt-2 float-right" %>
<% end %>
