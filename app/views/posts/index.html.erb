<% provide(:title, "Posts Index") %>
<% provide(:intro, "全ての投稿を閲覧できます") %>
<% @posts.each do |post| %>
  <div class="card">
    <div class="card-body">
      <h4><%= link_to(post.content, "/posts/#{post.id}") %></h4> 
    </div>
  </div>
  <br/>
<% end %>
