<% provide(:title, "Update post") %>

<%= form_for(@post) do |f| %>
  <%= f.label :content %>
  <%= f.text_area :content, class: "form-control" %>
  <%= f.submit "Update", class: "btn btn-primary mt-2 float-right"  %>
<% end %>  