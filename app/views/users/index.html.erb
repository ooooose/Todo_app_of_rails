<% provide(:title, "Users index") %>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to(user.name, user ) %> | <%= user.email %>
    </li>
  <% end %>
</ul>
<%= link_to("Create new user", new_user_path) %>
<%= link_to("Postsを見る", posts_path) %>
